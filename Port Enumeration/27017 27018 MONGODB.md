```md
# NoSQL - MongoDB

# Tool Used
sudo apt install mongodb-clients -y
mongo

# command used for listing all the databases present on the MongoDB server
show dbs
show dbs

# Interact with mongo
show dbs
use <db>
show collections

# Dump the collection
db.<collection>.find()

# Dump the collection Readable format
db.<collection>.find().pretty()

# Number of records of the collection
db.<collection>.count() 

# Find in current db the username admin
db.current.find({"username":"admin"}) 
```